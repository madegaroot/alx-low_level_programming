#include <stdio.h>


/**
 * main - prints the alphabet in lowecase using putchar function
 *
 * Return: 0
 */

int main(void);
{
	char c;

	for (c = 'a'; c <= 'z'; c++);
	putchar(c);
	putchar('\n');
	return (0);
}

